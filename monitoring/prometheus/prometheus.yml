global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'shopflow'
    metrics_path: '/actuator/prometheus'
    dns_sd_configs:
      - names:
          - 'app'
        type: 'A'
        port: 8080